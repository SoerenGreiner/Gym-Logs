<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Gym_Logs.ViewModel"
             x:Class="Gym_Logs.View.Controls.FlyoutItemView"
             HeightRequest="48"
             HorizontalOptions="Fill">
    <StackLayout>
        <Border x:Name="TapBorder"
                HeightRequest="45"
                HorizontalOptions="Fill"
                Margin="0"
                Padding="10,6">

            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{DynamicResource SurfaceSoft}" Offset="0.0"/>
                    <GradientStop Color="{DynamicResource Surface}" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>

            <Border.Triggers>
                <DataTrigger TargetType="Border"
                 Binding="{Binding IsPressed}"
                 Value="True">
                    <Setter Property="Background"
                Value="{DynamicResource AccentColor}" />
                </DataTrigger>
            </Border.Triggers>

            <Grid ColumnDefinitions="Auto,*" VerticalOptions="Center">
                <Image Source="{Binding Icon}"
               WidthRequest="22"
               HeightRequest="22"
               VerticalOptions="Center"/>

                <Label Grid.Column="1"
               Text="{Binding Title}"
               Margin="10,0,0,0"
               FontAttributes="Bold"
               TextColor="{DynamicResource PrimaryTextColor}"
               VerticalOptions="Center"/>
            </Grid>

            <Border.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:AppShellViewModel}}, Path=NavigateCommand}"
                                      CommandParameter="{Binding}" />
            </Border.GestureRecognizers>
        </Border>
        <BoxView HeightRequest="1"
                 BackgroundColor="{DynamicResource Glow}"
                 HorizontalOptions="Fill"/>
    </StackLayout>
</ContentView>
